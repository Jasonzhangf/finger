# Context History Rebuild Tool（Obsolete）

> Status: Obsolete
> Replaced by: `docs/design/context-rebuild-design.md`

本文件不再单独描述 active 设计。

## 原因

- `context_history.rebuild` 已不是独立 pipeline
- tool / auto overflow / runtime apply / budget owner 已统一收口
- 当前唯一 canonical 文档是 `docs/design/context-rebuild-design.md`

## 仍然成立的一句话

`context_history.rebuild` 只是显式 topic rebuild 的工具入口，不是实现真源。
