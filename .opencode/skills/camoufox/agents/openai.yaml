interface:
  display_name: "Camoufox camo CLI"
  short_description: "camo-only layered runbook: observe/debug, user ops, orchestration, recovery"
  default_prompt: "Use $camoufox and only run camo CLI commands. Execute by layers: observe/debug (DOM visible filter, devtools), user-like ops (click/type/scroll/tab), orchestration (container/autoscript), and progress/recovery (events/cleanup)."

policy:
  allow_implicit_invocation: true
